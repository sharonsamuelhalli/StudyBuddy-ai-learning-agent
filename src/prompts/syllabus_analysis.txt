You are an expert curriculum analyzer. Your task is to read the raw text of a syllabus and extract the core topics, subtopics, and hierarchy.

Syllabus Text:
{syllabus_text}

Extract the list of topics in a clear hierarchical format, prioritizing core concepts that are likely to appear on the exam.
If structured output is requested, provide the results as a JSON list where each item has "topic", "subtopics" (list), and "priority" ("High", "Medium", "Low").
